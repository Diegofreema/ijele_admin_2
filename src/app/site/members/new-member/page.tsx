import { NextPage } from 'next';
import { AddNewMember } from '../_component/AddNewMember';

interface Props {}

const page: NextPage<Props> = ({}): JSX.Element => {
  return <AddNewMember />;
};

export default page;
